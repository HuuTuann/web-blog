export enum RegisterKeys {
  FULLNAME = "full_name",
  PASSWORD = "password",
  CONFIRM_PASSWORD = "confirm_password",
  EMAIL = "email",
  AVATAR = "avatar",
  ROLE = "roleId",
}

export enum RegisterRole {
  ADMIN = 1,
  BUSINESS = 2,
  CANDIDATE = 3,
}
